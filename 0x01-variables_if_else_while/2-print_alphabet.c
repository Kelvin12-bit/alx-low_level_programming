#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - User entry
 * Return: 0
 */
int main(void)
{
char lower;
for (lower = 'a'; lower <= 'z'; lower++)
	{
		putchar(lower);
		putchar ('\n');
	}
				return (0);
}
